import Link from "next/link";import { redirect } from "next/navigation";import { requireChatGPTUser,chatGPTSignOutPath } from "../../chatgpt-auth";import { database } from "../../db";import { Eye,MapPin,BriefcaseBusiness,CalendarDays } from "lucide-react";export const dynamic="force-dynamic";
type W={id:string;reference:string;full_name:string;area:string;work_type:string;services:string;languages:string;experience_years:number;status:string};type V={id:string;viewed_at:string;full_name:string;area:string;service:string;status:string};export default async function Dashboard(){const u=await requireChatGPTUser("/worker/dashboard"),db=database(),w=await db.prepare("SELECT id,reference,full_name,area,work_type,services,languages,experience_years,status FROM workers WHERE user_id=?").bind(u.userId).first<W>();if(!w)redirect("/account/claim");const views=(await db.prepare("SELECT pv.id,pv.viewed_at,c.full_name,c.area,c.service,c.status FROM profile_views pv JOIN clients c ON c.id=pv.client_id WHERE pv.worker_id=? ORDER BY pv.viewed_at DESC LIMIT 100").bind(w.id).all<V>()).results;return <main className="portal-bg"><header className="portal-top"><div className="shell portal-nav"><Link className="brand" href="/"><span className="brand-mark">MC</span>Maid Center Zambia</Link><div><span>{u.email}</span><a href={chatGPTSignOutPath("/")}>Sign out</a></div></div></header><div className="shell portal-layout"><aside className="portal-side"><span className="avatar-initial">{w.full_name.charAt(0)}</span><h2>{w.full_name}</h2><span className="verified-line">Approved professional</span><nav><a className="active" href="#overview">Overview</a><a href="#views">Profile views</a><a href="#profile">My profile</a></nav></aside><section className="portal-content"><div className="portal-heading"><div><span className="overline">Worker dashboard</span><h1>Welcome, {w.full_name.split(" ")[0]}</h1><p>Track client interest and keep your professional information current.</p></div><span className="reference-chip">{w.reference}</span></div><div className="portal-stats"><div><Eye/><strong>{views.length}</strong><span>Profile views</span></div><div><BriefcaseBusiness/><strong>{w.experience_years}</strong><span>Years of experience</span></div><div><MapPin/><strong>{w.area}</strong><span>Preferred area</span></div></div><section className="portal-card" id="views"><div className="card-title"><div><h2>Who viewed your profile</h2><p>Only verified clients are recorded here.</p></div></div>{views.length?<div className="viewer-list">{views.map(v=><article key={v.id}><span className="viewer-avatar">{v.full_name.charAt(0)}</span><div><strong>{v.full_name}</strong><small>{v.service} · {v.area}</small></div><time><CalendarDays size={15}/>{new Date(v.viewed_at).toLocaleDateString("en-ZM",{day:"numeric",month:"short",year:"numeric"})}</time></article>)}</div>:<div className="professional-empty"><Eye/><h3>No profile views yet</h3><p>When a verified client views your profile, their name, service request and area will appear here.</p></div>}</section><section className="portal-card" id="profile"><h2>Professional profile</h2><dl className="profile-dl"><div><dt>Work arrangement</dt><dd>{w.work_type}</dd></div><div><dt>Services</dt><dd>{w.services}</dd></div><div><dt>Languages</dt><dd>{w.languages}</dd></div><div><dt>Verification</dt><dd>Approved by Maid Center Zambia</dd></div></dl></section></section></div></main>}
