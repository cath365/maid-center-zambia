import Link from "next/link";import { redirect } from "next/navigation";import { requireChatGPTUser,chatGPTSignOutPath } from "../chatgpt-auth";import { database } from "../db";import { MapPin,BriefcaseBusiness,Languages,ShieldCheck } from "lucide-react";export const dynamic="force-dynamic";type W={id:string;full_name:string;area:string;work_type:string;services:string;languages:string;experience_years:number};export default async function Professionals(){const u=await requireChatGPTUser("/professionals"),db=database(),client=await db.prepare("SELECT id,full_name,status FROM clients WHERE user_id=?").bind(u.userId).first<{id:string;full_name:string;status:string}>();if(!client)redirect("/account/claim");const workers=(await db.prepare("SELECT id,full_name,area,work_type,services,languages,experience_years FROM workers WHERE status='approved' ORDER BY created_at DESC").all<W>()).results;return <main className="directory-bg"><header className="directory-top"><div className="shell portal-nav"><Link className="brand dark-brand" href="/"><span className="brand-mark">MC</span>Maid Center Zambia</Link><div><span>{client.full_name}</span><a href={chatGPTSignOutPath("/")}>Sign out</a></div></div></header><div className="shell directory-main"><div className="directory-heading"><div><span className="overline">Verified directory</span><h1>Household professionals</h1><p>Profiles are visible only to verified clients. Contact details remain private until Maid Center confirms a match.</p></div><Link className="outline-button" href="/">Submit another request</Link></div><div className="directory-grid">{workers.map(w=><Link className="professional-card" key={w.id} href={`/professionals/${w.id}`}><div className="profile-head"><span className="profile-initial">{w.full_name.charAt(0)}</span><span className="verified-badge"><ShieldCheck size={14}/> Verified</span></div><h2>{w.full_name}</h2><p>{w.services}</p><ul><li><MapPin/>{w.area}</li><li><BriefcaseBusiness/>{w.experience_years} years · {w.work_type}</li><li><Languages/>{w.languages}</li></ul><span className="profile-link">View professional profile</span></Link>)}{!workers.length&&<div className="professional-empty wide"><h3>No approved profiles are available yet</h3><p>Approved workers will appear here automatically.</p></div>}</div></div></main>}
