"use client";import { useState } from "react";import { useRouter } from "next/navigation";import { toast } from "sonner";import { Toaster } from "@/components/ui/sonner";import { Tabs,TabsContent,TabsList,TabsTrigger } from "@/components/ui/tabs";
type Worker={id:string;reference:string;full_name:string;phone:string;area:string;work_type:string;services:string;status:string;created_at:string};type Client={id:string;reference:string;full_name:string;phone:string;area:string;service:string;budget:number;status:string;created_at:string};type Match={id:string;status:string;created_at:string;worker_name:string;client_name:string};
export function AdminControls({workers,clients,matches}:{workers:Worker[];clients:Client[];matches:Match[]}){const router=useRouter(),[w,setW]=useState(""),[c,setC]=useState("");async function act(body:object){const r=await fetch("/api/admin",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),j=await r.json();if(!r.ok){toast.error(j.error||"Action failed");return}toast.success(j.message);router.refresh()}return <><Toaster richColors/><Tabs defaultValue="workers"><TabsList><TabsTrigger value="workers">Workers</TabsTrigger><TabsTrigger value="clients">Clients</TabsTrigger><TabsTrigger value="matches">Matches</TabsTrigger></TabsList><TabsContent value="workers"><div className="admin-card"><h2>Worker applications</h2>{workers.length?<table className="data-table"><thead><tr><th>Applicant</th><th>Location</th><th>Services</th><th>Status</th><th>Actions</th></tr></thead><tbody>{workers.map(x=><tr key={x.id}><td><strong>{x.full_name}</strong><br/>{x.reference}<br/>{x.phone}</td><td>{x.area}<br/>{x.work_type}</td><td>{x.services}</td><td><span className={`status ${x.status}`}>{x.status}</span></td><td><div className="admin-actions"><button onClick={()=>act({type:"worker",id:x.id,status:"screening"})}>Screen</button><button onClick={()=>act({type:"worker",id:x.id,status:"approved"})}>Approve</button><button onClick={()=>act({type:"worker",id:x.id,status:"rejected"})}>Reject</button></div></td></tr>)}</tbody></table>:<div className="empty">No applications yet.</div>}</div></TabsContent><TabsContent value="clients"><div className="admin-card"><h2>Client requests</h2>{clients.length?<table className="data-table"><thead><tr><th>Client</th><th>Request</th><th>Budget</th><th>Status</th><th>Actions</th></tr></thead><tbody>{clients.map(x=><tr key={x.id}><td><strong>{x.full_name}</strong><br/>{x.reference}<br/>{x.phone}</td><td>{x.area}<br/>{x.service}</td><td>K{x.budget}</td><td><span className={`status ${x.status}`}>{x.status}</span></td><td><div className="admin-actions"><button onClick={()=>act({type:"client",id:x.id,status:"verified"})}>Verify</button><button onClick={()=>act({type:"client",id:x.id,status:"matching"})}>Start matching</button><button onClick={()=>act({type:"client",id:x.id,status:"closed"})}>Close</button></div></td></tr>)}</tbody></table>:<div className="empty">No requests yet.</div>}</div></TabsContent><TabsContent value="matches"><div className="admin-card"><h2>Create a match</h2><div className="form-grid"><label className="field"><span>Approved worker</span><select value={w} onChange={e=>setW(e.target.value)}><option value="">Choose worker</option>{workers.filter(x=>x.status==="approved").map(x=><option key={x.id} value={x.id}>{x.full_name} — {x.area}</option>)}</select></label><label className="field"><span>Open client request</span><select value={c} onChange={e=>setC(e.target.value)}><option value="">Choose client</option>{clients.filter(x=>!["closed","placed"].includes(x.status)).map(x=><option key={x.id} value={x.id}>{x.full_name} — {x.service}</option>)}</select></label><button className="button full" disabled={!w||!c} onClick={()=>act({type:"match",workerId:w,clientId:c})}>Create shortlist match</button></div></div><div className="admin-card"><h2>Placement pipeline</h2>{matches.length?<table className="data-table"><thead><tr><th>Worker</th><th>Client</th><th>Status</th><th>Actions</th></tr></thead><tbody>{matches.map(x=><tr key={x.id}><td>{x.worker_name}</td><td>{x.client_name}</td><td><span className={`status ${x.status}`}>{x.status}</span></td><td><div className="admin-actions"><button onClick={()=>act({type:"matchStatus",id:x.id,status:"interview"})}>Interview</button><button onClick={()=>act({type:"matchStatus",id:x.id,status:"placed"})}>Placed</button><button onClick={()=>act({type:"matchStatus",id:x.id,status:"declined"})}>Declined</button></div></td></tr>)}</tbody></table>:<div className="empty">No matches created yet.</div>}</div></TabsContent></Tabs></>}
