import Link from "next/link";import { requireChatGPTUser } from "../../chatgpt-auth";import { ClaimForm } from "./claim-form";export const dynamic="force-dynamic";export default async function Claim(){const user=await requireChatGPTUser("/account/claim");return <main className="portal-bg"><header className="portal-top"><div className="shell portal-nav"><Link className="brand" href="/"><span className="brand-mark">MC</span>Maid Center Zambia</Link><span>{user.email}</span></div></header><section className="claim-shell"><div className="claim-card"><span className="overline">Account activation</span><h1>Connect your approved record</h1><p>Use the application reference and phone number submitted during registration. Worker accounts require administrator approval; client accounts require verification.</p><ClaimForm/></div></section></main>}
